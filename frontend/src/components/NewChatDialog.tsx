import { Bot, Settings, X } from 'lucide-react';
import type { Agent } from '../types';

type Props={open:boolean;onClose:()=>void;onCreate:(agentId?:string,model?:string)=>void;defaultAgentId?:string;defaultModel?:string;onManageAgents:()=>void};
export function NewChatDialog({open,onClose,onCreate,defaultAgentId,defaultModel,onManageAgents}:Props){if(!open)return null;return <div className="modal-backdrop" onClick={onClose}><div className="modal new-chat-modal" onClick={e=>e.stopPropagation()}><div className="modal-head"><div><h3>New chat</h3><p>使用已设置的默认 Agent 和模型开始对话。</p></div><button className="icon-button" onClick={onClose}><X size={16}/></button></div><div className="model-picker"><div className="selected-agent"><div className="agent-choice-icon"><Bot size={17}/></div><div><strong>{defaultAgentId||'未设置默认 Agent'}</strong><span>{defaultModel||'使用 Agent 默认模型'}</span></div></div>{defaultAgentId?<button className="primary-button create-chat-button" onClick={()=>onCreate()}><Bot size={15}/> 开始对话</button>:<><div className="modal-empty"><p>还没有设置默认 Agent，请先到 Agents & runtimes 设置。</p></div><button className="primary-button" onClick={onManageAgents}><Settings size={15}/> 设置默认 Agent</button></>}</div></div></div>}
